// using the api :-> https://type.fit/api/quotes
// or search for 'quotes generation api free'

// on refershing our page or clicking new quotes button one will automatically get new quotes

//fetch() funciton will call the http of api
      // the moment response comes it is saved in 'data' variable
    //   console.log(realData[0].text);
     
    //total 1643 quotes on api